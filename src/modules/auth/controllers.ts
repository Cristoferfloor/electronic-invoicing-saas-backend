// Controladores de autenticación
// login(email, password) - Validar credenciales y generar JWT
// register(userData) - Crear nuevo usuario
// refreshToken(refreshToken) - Generar nuevo access token
